<h1>Editando usuario</h1>

<% form_for(@usuario) do |f| %>

	<div class="field">
		<%= f.label :nombre %><br>
		<%= f.text_field :nombre %>
	</div>
	<div> class="actions"><%= f.submit "guardar cambios" %>
	</div>
<% end %>

<%= link_to "Atras", users_path %>